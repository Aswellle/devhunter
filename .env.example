# DevHunter Docker Compose 环境变量
# 复制为 .env 并修改后运行 docker compose up -d

# 访问密码（必须修改）
AUTH_PASSWORD=devhunter123

# JWT 密钥（生产环境请用 openssl rand -hex 32 生成）
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
